# Copy to .env.local to override. By default the app calls "/api", which the
# Vite dev server proxies to http://localhost:8000 (see vite.config.js).
# Set this only if the backend is hosted elsewhere.
VITE_API_BASE=/api
